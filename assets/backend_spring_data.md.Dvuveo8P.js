import{_ as e,o as t,c as i,a4 as s}from"./chunks/framework.DkKDtR5x.js";const g=JSON.parse('{"title":"Spring Data","description":"","frontmatter":{},"headers":[],"relativePath":"backend/spring/data.md","filePath":"backend/spring/data.md"}'),n={name:"backend/spring/data.md"};function r(l,a,o,d,c,h){return t(),i("div",null,[...a[0]||(a[0]=[s(`<h1 id="spring-data" tabindex="-1">Spring Data <a class="header-anchor" href="#spring-data" aria-label="Permalink to “Spring Data”">​</a></h1><h2 id="spring-data-jpa" tabindex="-1">Spring Data JPA <a class="header-anchor" href="#spring-data-jpa" aria-label="Permalink to “Spring Data JPA”">​</a></h2><p><code>spring-data-jpa</code></p><div class="language-conf"><button title="Copy Code" class="copy"></button><span class="lang">conf</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>logging.level.org.hibernate.SQL=DEBUG</span></span>
<span class="line"><span>logging.level.org.hibernate.type.descriptor.sql.BasicBinder=DEBUG</span></span></code></pre></div><h3 id="saveall" tabindex="-1">saveAll <a class="header-anchor" href="#saveall" aria-label="Permalink to “saveAll”">​</a></h3><ul><li>Looking into the implementation of the two methods, we can see that <code>saveAll()</code> iterates over each element and uses the <code>save()</code> method in each iteration. This implies that it shouldn’t be such a big performance difference.</li><li>Both use <code>@Transactional</code> with default transaction propagation type is REQUIRED, which means that, if not provided, a new transaction is created each time the methods are called.</li><li>Each time we call the save() method, a new transaction is created, whereas when we call saveAll(), only one transaction is created, and it’s reused later by save().</li><li>This overhead translates into the performance difference that we noticed earlier. <ul><li>Finally, the overhead is bigger when batching is enabled due to the fact that it’s done at the transaction level.</li></ul></li></ul><div class="language-conf"><button title="Copy Code" class="copy"></button><span class="lang">conf</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span># HIBERNATE BATCHING PROPERTIES</span></span>
<span class="line"><span>spring.jpa.properties.hibernate.jdbc.batch_size=250</span></span>
<span class="line"><span>spring.jpa.properties.hibernate.order_inserts=true</span></span></code></pre></div>`,7)])])}const b=e(n,[["render",r]]);export{g as __pageData,b as default};
